import 'package:flutter/material.dart';
import 'components/bus_booking_body.dart';

class BusBooking extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: BusBookingBody(),
    );
  }
}
